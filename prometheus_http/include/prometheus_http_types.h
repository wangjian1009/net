#ifndef PROMETHEUS_HTTP_TYPES_H_INCLEDED
#define PROMETHEUS_HTTP_TYPES_H_INCLEDED
#include "prometheus_types.h"

CPE_BEGIN_DECL

typedef struct prometheus_http_processor * prometheus_http_processor_t;

CPE_END_DECL

#endif
