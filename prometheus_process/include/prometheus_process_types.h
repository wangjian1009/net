#ifndef PROMETHEUS_PROCESS_TYPES_H_INCLEDED
#define PROMETHEUS_PROCESS_TYPES_H_INCLEDED
#include "prometheus_types.h"

CPE_BEGIN_DECL

typedef struct prometheus_process_provider * prometheus_process_provider_t;

CPE_END_DECL

#endif
